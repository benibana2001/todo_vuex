import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

type Payload = {
  amount: number;
};

const store = new Vuex.Store({
  state: {},
  mutations: {},
  getters: {},
  actions: {},
  modules: {},
});

export default store;
